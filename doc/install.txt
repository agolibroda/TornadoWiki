
 что нужно для работы системы.
 
 - торнадо 
 - дравера для MySQL ()
 	ставится таким образом:
 	
 	pip install tornado
 	pip install pymysql
 	pip install bcrypt
 	
 - инсталляция базы данных
 проверить, (и создать, если нет) директорию "filestorage" в "static" - тада будут складываться занружаемые файлы
 
 убрал я работу с мускулом, 
 
 pip uninstall pymysql
 
 надо поставить какой - то пакет для работы с Postgre  
  трахатся уже с ней. 
  
  PostgreSql user: postgres pwd: 123
  
  поставить ервер
  поставить дравер между питоним и сервером
  
  